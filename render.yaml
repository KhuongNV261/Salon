services:
  - type: web
    name: localpos-backend
    runtime: python
    rootDir: backend
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn main:app --bind 0.0.0.0:$PORT --workers 2 --timeout 60
    envVars:
      - key: DATABASE_URL
        value: postgresql://neondb_owner:npg_nsE3jBrdlxo8@ep-rough-firefly-azgm5tgr-pooler.c-3.ap-southeast-1.aws.neon.tech/neondb?sslmode=require&channel_binding=require
      - key: SECRET_KEY
        value: localpos-prod-secret-2026-khuong2601
    healthCheckPath: /api/health
    plan: free
